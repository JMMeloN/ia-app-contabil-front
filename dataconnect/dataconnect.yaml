specVersion: "v1beta"
serviceId: "iacontabil"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "iacontabil-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
